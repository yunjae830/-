<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mailbox">
	
	<select id="getList" resultType="MailboxDto">
		SELECT * FROM TBL_MAILBOX ORDER BY BNO DESC
	</select>
	
	<select id="getMail" resultType="MailboxDto">
		SELECT * FROM TBL_MAILBOX
		WHERE BNO = #{bno}
	</select>
	
	<insert id="addMail" parameterType="MailboxDto">
		INSERT INTO TBL_MAILBOX (BNO, RECIPIENT, TITLE, CONTENT) VALUES(
			SEQ_MAILBOX.NEXTVAL, #{recipient}, #{title}, #{content}
		)
	</insert>
	
	<insert id="insertSelectKey" parameterType="MailboxDto">
		
		<selectKey keyProperty="bno" order="BEFORE" resultType="int">
			SELECT SEQ_MAILBOX.NEXTVAL FROM DUAL
		</selectKey>
		
		INSERT INTO TBL_MAILBOX(BNO, RECIPIENT, TITLE, CONTENT)
		VALUES(#{bno}, #{recipient}, #{title}, #{content})
		
	</insert>
	
	<delete id="deleteMail">
		DELETE FROM TBL_MAILBOX WHERE BNO = #{bno}
	</delete>
	
	<update id="updateMail" parameterType="MailboxDto">
		UPDATE TBL_MAILBOX
		SET RECIPIENT = #{recipient},
		TITLE = #{title},
		CONTENT = #{content}
		WHERE BNO = #{bno}
	</update>
	
	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach item='type' collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							TITLE LIKE '%'||#{keyword}||'%'
						</when>
						<when test="type == 'C'.toString()">
							CONTENT LIKE '%'||#{keyword}||'%'
						</when>
						<when test="type == 'R'.toString()">
							RECIPIENT LIKE '%'||#{keyword}||'%'
						</when>
					</choose>					
				</trim>
			</foreach>
		</trim>
	</sql>
	
	<select id="getListWithPaging" resultType="MailboxDto">
	<![CDATA[
		SELECT BNO, WRITER, RECIPIENT, TITLE, CONTENT, REGDATE
		FROM
		(
		SELECT /*+INDEX_DESC(TBL_MAILBOX PK_MAILBOX)*/
		ROWNUM RN, BNO, WRITER, RECIPIENT, TITLE, CONTENT, REGDATE
		FROM
			TBL_MAILBOX
		WHERE 

		]]>
		
		<include refid="criteria"/>
		
		<![CDATA[
				ROWNUM <= #{pageNum} * #{amount}
		)
		WHERE RN > (#{pageNum} -1) * #{amount}
		]]>
		
	</select>
	
	<select id="getTotalCount" resultType="int">
		SELECT COUNT(*) FROM TBL_MAILBOX WHERE
		
		<include refid="criteria"/>
		
		BNO > 0
	</select>



</mapper>
